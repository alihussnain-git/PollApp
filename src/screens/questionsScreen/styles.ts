import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  flatList: {
    marginTop: 12,
  },
  flatListContainer: {
    paddingBottom: 86,
  },
  progressIndicator: {
    alignSelf: 'center',
    marginTop: 16,
  },
});

export default styles;
