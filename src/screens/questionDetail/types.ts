import { StackScreenProps } from '@react-navigation/stack';
import { RootStackParamList } from '../../navigation/stack_navigator';

export type QuestionDetailProps = StackScreenProps<RootStackParamList, 'QuestionDetail'>;
